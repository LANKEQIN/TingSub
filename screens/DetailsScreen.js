import React from 'react';
import { View } from 'react-native';
import { Text } from 'tamagui';

const DetailsScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text fontSize={24}>详情页</Text>
    </View>
  );
};

export default DetailsScreen;